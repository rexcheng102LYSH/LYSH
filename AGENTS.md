# Project Lysh - AI Interaction Rules (AGENTS.md)
NOTE FOR AI: Also read `AGENTS_CONTEXT.md` for the file map and logic overview.

你好AI，我是项目负责人。这里是 Project Lysh（技能版五子棋）的核心开发规则。
作为长期 Web 项目，请在生成或修改代码前，严格遵守以下规则。

## 1. 项目目标 (Project Goals)
- **核心定义**：本项目是一个基于 Web (HTML/CSS/JS) 的“技能版五子棋”游戏，最终目标为上架 io 网站或 Steam 平台。
- **核心体验**：在传统五子棋基础上加入技能（如炸弹、时间控制）、动态 UI 与视觉特效。
- **用户画像**：面向喜欢策略与视觉反馈的玩家。
- **当前阶段**：Alpha 阶段，核心逻辑已构建，重点是新增特性时的稳定性，而非重构。

## 2. 技术偏好 (Tech Stack & Preferences)

### 前端技术栈 (Frontend Stack)
- **核心语言**：原生 JavaScript (Vanilla JS), HTML5, CSS3
- **模块化架构**：ES6模块化语法，按功能划分（audio, fx, game, network, skills）
- **图形渲染**：Canvas API（背景渲染 bgCanvas + 特效渲染 fxCanvas）
- **动画控制**：requestAnimationFrame + 统一帧率控制器（60fps）
- **实时通信**：Socket.IO客户端（联网对战功能）
- **UI 框架**（Alpha 0.7.9.6）：Tailwind CSS（样式）+ Anime.js（动画），仅用于新增 UI，零风险

### 后端技术栈 (Backend Stack)
- **运行环境**：Node.js
- **Web框架**：Express.js
- **实时通信**：Socket.IO服务器
- **跨域处理**：CORS

### 开发工具 (Development Tools)
- **本地开发**：VS Code Live Server
- **编码规范**：UTF-8 编码，确保多语言支持

### 架构设计原则
- **模块化架构**：遵循单一职责原则，每个模块专注特定功能
- **状态集中管理**：使用统一对象管理游戏状态，提高代码可维护性
- **向后兼容性**：保留历史全局变量，确保平滑过渡

## 3. 代码风格 (Code Style)
- **完整性**：输出代码时，严禁使用 `// ... rest of code` 或 `// ... previous code` 省略。
- **结构与命名**：优先保持现有结构与命名习惯（如 `GameState`, `updateDynamicUI`），不要为“规范化”随意改名。
- **注释**：关键逻辑需保留中文注释；修改代码时请在改动处添加简短注释说明原因。

## 4. 风险偏好 (Risk Tolerance)
- **稳定性优先**：优先修复 bug、性能隐患与逻辑不一致问题。
- **保守**：未经确认不要修改主要交互流程。
- **状态同步**：`GameState` 为主要状态源，改动需同步旧全局变量以保持兼容。
- **新增功能原则**：尽量通过“新增函数”或“在末尾追加逻辑”实现，避免触碰核心判定逻辑（如胜利判定）。
- **重构须知**：涉及大更新或大改动前，请先充分沟通确认。

## 5. 测试/验证 (Testing Strategy)
- **环境**：使用 VS Code Live Server 进行本地浏览器预览。
- **验证指引**：每次给出代码后，请简要说明要测试的具体功能（例如“黑子落子后计时器是否变色”）。
- **手测优先**：提供可执行的手测步骤（进入游戏、选技能、触发撤销等）。
- **无法测试**：若无法运行测试，请明确说明并给出验证建议。

## 6. 输出习惯 (Output Habits)
- **操作指引**：说明修改了哪个文件的哪一部分，或是否替换了整个文件。
- **表达顺序**：先说明改动目的，再列具体改动点与文件路径。
- **语言**：中文输出，简洁明确；有疑问先问。

## 7. 语言与沟通 (Language & Communication)
- **语言**：请使用中文沟通。
- **风格**：尽量有趣生动，解释原理时请通俗易懂，照顾我作为非专业程序员的理解能力。

## 8. 禁区 (Forbidden Zones)
- **严禁偷懒**：这是最大的禁区，进行大规模任务时，比如在模块化扩展时要确保准确的搬运所有代码，不要遗漏任何一个起作用的老旧代码。
- **严禁擅自升级**：不要改动游戏版本号！不要擅自增加游戏版本号！除非我指示。
- **严禁乱改**：进行任何代码改动前先告知并询问我的意见。
- **不要PowerShell编码**:永远拒绝PowerShell编码,那会让游戏乱码!
- **【强制】UTF-8 编码 (Enforce UTF-8)**：
    - **规则**：生成的任何文本内容、代码文件必须严格强制为 `UTF-8` 编码。
    - **防乱码机制**：严禁依赖操作系统默认编码（如 Windows 的 GBK/CP1252），严禁在不确定的编码环境下写入 Emoji，这会导致文件被 `NUL` 字节损坏。
    - **Emoji 处理**：涉及 Emoji 或中文时，直接在代码块中输出原始字符，禁止尝试通过脚本进行复杂的 Unicode 转换。
---
AI，请确认你已理解上述规则，并在后续协助中严格执行。
